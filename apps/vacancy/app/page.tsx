import { AppShell } from "@/components/AppShell";

/**
 * Leegstandsradar — main page
 *
 * Server component. The actual map and interactive state live in
 * client components below — this page just composes the shell.
 */
export default function Page() {
  return <AppShell />;
}
